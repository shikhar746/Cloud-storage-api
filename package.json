{
  "name": "cloud-storage",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud storage API and web client, built together so one host can serve both from a single origin.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix web install && npm --prefix web run build && npm --prefix api install && npm --prefix api run build",
    "start": "npm --prefix api start"
  }
}
